$files = Get-ChildItem -Path "c:\Users\vedan\campusconnect-frontend\app" -Recurse -Filter "page.tsx" | Where-Object { $_.FullName -match "\[.*\]" }; foreach ($f in $files) { $content = Get-Content $f.FullName -Raw; if ($content -notmatch "generateStaticParams") { $content += "`n`nexport function generateStaticParams() { return []; }`n"; Set-Content $f.FullName $content; Write-Host "Updated $($f.FullName)" } }
